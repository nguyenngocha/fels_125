<% provide title: @category.title %>
<% provide :button_text, t("add_word") %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <span class="categories"><%= @category.title %></span>
    <p class="description"><%= @category.description %></p>
    <%= render "admin/words/form" %>
  </div>
</div>
